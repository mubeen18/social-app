<%= form_with model: [ article, article.comments.build ] do |form| %>
  <p>
    <%= form.label :comment_body %><br>
    <%= form.text_area :comment_body %>
  </p>
  <p>
    <%= form.submit %>
  </p>
<% end %>

